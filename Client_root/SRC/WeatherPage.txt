export default function WeatherPage() {
  const cities = [
    { name: "Mumbai (Andheri)", sector: "Traffic & Urban Mobility", forecast: "42mm rain expected", risk: "HIGH (Flood Zone)", statusBadge: "bg-rose-500/10 text-rose-400 border-rose-500/20" },
    { name: "Delhi (Central)", sector: "Public Health & Heatwave", forecast: "41°C / 65% Humidity", risk: "SEVERE (Extreme Caution)", statusBadge: "bg-amber-500/10 text-amber-400 border-amber-500/20" }
  ];

  return (
    <div className="max-w-4xl w-full mx-auto p-6 space-y-6">
      <div>
        <h1 className="text-2xl font-bold tracking-tight">Active Sector Weather & Threshold Monitor</h1>
        <p className="text-slate-400 text-sm">Real-time Open-Meteo forecasts mapped against deterministic safety rules.</p>
      </div>

      <div className="grid gap-4 md:grid-cols-2">
        {cities.map((c, i) => (
          <div key={i} className="bg-slate-900 border border-slate-800 rounded-xl p-5 space-y-3">
            <div className="flex justify-between items-start">
              <h3 className="font-semibold text-lg text-slate-200">{c.name}</h3>
              <span className={`text-xs px-2.5 py-1 rounded-full border ${c.statusBadge}`}>
                {c.risk}
              </span>
            </div>
            <div className="text-xs text-indigo-400 font-medium">{c.sector}</div>
            <div className="text-sm text-slate-300 bg-slate-950 p-3 rounded-lg border border-slate-800">
              <span className="text-slate-500 block text-xs mb-1">Live Forecast:</span>
              {c.forecast}
            </div>
          </div>
        ))}
      </div>
    </div>
  );
}